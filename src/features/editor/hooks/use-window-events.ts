// Disabled browser beforeunload alert for better UX
// The app now auto-saves changes, eliminating the need for this warning
export const useWindowEvents = () => {
  // No longer showing browser alerts - the app handles saving automatically
};
